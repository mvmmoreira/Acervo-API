spring:
  application:
    name: acervo-pessoal-api
  datasource:
    url: jdbc:h2:mem:apiacervopessoal
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  h2:
    console:
      enabled: true
      path: /h2-console
